import 'package:flutter/material.dart';

class JDrawer extends StatelessWidget {
  const JDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}            