import 'package:flutter/material.dart';

class ScnTempHome extends StatelessWidget {
  const ScnTempHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome Home'),
      )
    );
  }
}