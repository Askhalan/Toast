import 'package:flutter/material.dart';

class AUserManagement extends StatelessWidget {
  const AUserManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: ,
    );
  }
} 